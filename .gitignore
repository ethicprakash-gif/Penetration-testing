# Dependencies
/node_modules
package-lock.json
yarn.lock
bun.lockb

# Docusaurus build output
/build
/.docusaurus
.cache-loader

# Generated content (produced by `bun scripts/migrate.ts` — source of truth is the
# original content folders at the repo root; docs/ and static/pdfs/ are rebuilt in CI)
/docs
/static/pdfs

# Misc
.DS_Store
.env
.env.local
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea
.vscode/*
!.vscode/extensions.json

# Internal build report (kept local, not published)
MIGRATION_REPORT.md
